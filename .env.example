# Supabase (스냅샷 저장용 — 서버사이드)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase (클라이언트용 — 현재 미사용)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# 스냅샷 수집 인증 토큰 (POST /api/snapshot 호출 시 Bearer 토큰)
COLLECTOR_SECRET=your-collector-secret

# GitHub API 토큰 (선택 — 없으면 시간당 60회 제한, 있으면 5000회)
# https://github.com/settings/tokens 에서 발급 (public_repo 권한)
GITHUB_TOKEN=

# 로컬 서비스 URL (기본값이 있으므로 변경 시에만 설정)
# MAKE_MONEY_API_URL=http://localhost:3001
# TELEGRAM_BOT_API_URL=http://localhost:5001
# OPENCLAW_HEALTH_URL=http://localhost:7100
